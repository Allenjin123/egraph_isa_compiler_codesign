(include "base.egg")

(let a (RegVal "a"))
(let b (RegVal "b"))

; Create a Seq2
(let myseq (Seq2 (Nop) (Nop)))

; Create the expanded version manually
(let expanded (Cons (Nop) (Cons (Nop) (Nil))))

; Run
(run 5)

; Extract both to see what they look like
(extract myseq)
(extract expanded)

; See if Seq2 rewrite worked
(check (= myseq expanded))
